namespace App.Net
{
    public interface IProcessService
    {
        void SuspendProcess(string username);
        void ResumeProcess(string username);
        void RestrictProcessCPU(int processid, string processname);
        void RestrictProcessMemory(int processid, string processname);
        void RemoveRestriction(int processid, string processname);
    }
}
